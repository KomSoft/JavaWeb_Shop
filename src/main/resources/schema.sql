CREATE TABLE users (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
			login VARCHAR(50),
			full_name VARCHAR(255),
			password VARCHAR(64),
			region VARCHAR(32),
			gender CHARACTER(1),
			comment VARCHAR(255));
